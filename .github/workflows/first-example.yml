name: My First Workflow
on: [push]
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "Hello, World!"

            - name: List files
              run: ls -la

            - name: Read a file
              run: cat Readme.md